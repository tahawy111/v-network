import { IPost } from '@/types/typescript';
import { } from 'react';

interface CardBodyProps {
    post: IPost;
}

export default function CardBody({ }: CardBodyProps) {
    return <div>CardBody</div>;
}
